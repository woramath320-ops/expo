import React from "react"; import { Button } from "@/components/ui/button"; import { Card, CardContent } from "@/components/ui/card"; import { Input } from "@/components/ui/input"; import { Tabs, TabsList, TabsTrigger, TabsContent } from "@/components/ui/tabs"; import { Star, Search, Briefcase } from "lucide-react";

export default function WorkHubApp() { return ( <div className="min-h-screen bg-white text-blue-900 flex flex-col items-center p-4"> {/* Header */} <header className="w-full text-center py-4 border-b border-blue-200"> <h1 className="text-3xl font-bold text-blue-700">WorkHub</h1> <p className="text-sm text-blue-500">หางานง่าย ๆ รวมทุกบริการไว้ที่เดียว</p> </header>

{/* Tabs Navigation */}
  <Tabs defaultValue="home" className="w-full max-w-md mt-4">
    <TabsList className="grid grid-cols-3 gap-2 bg-blue-100 p-1 rounded-xl">
      <TabsTrigger value="home">🏠 หน้าหลัก</TabsTrigger>
      <TabsTrigger value="jobs">🧰 งาน</TabsTrigger>
      <TabsTrigger value="profile">👤 โปรไฟล์</TabsTrigger>
    </TabsList>

    {/* Home Tab */}
    <TabsContent value="home" className="mt-4">
      <div className="flex flex-col gap-3">
        <h2 className="text-xl font-semibold">หมวดหมู่งานยอดนิยม</h2>
        <div className="grid grid-cols-2 gap-3">
          {["งานช่าง", "ออกแบบ", "แม่บ้าน", "พิมพ์งาน", "ส่งของ", "อื่น ๆ"].map((cat) => (
            <Card key={cat} className="text-center p-3 shadow-sm border-blue-100">
              <CardContent>{cat}</CardContent>
            </Card>
          ))}
        </div>
      </div>
    </TabsContent>

    {/* Jobs Tab */}
    <TabsContent value="jobs" className="mt-4">
      <div className="flex items-center gap-2 mb-3">
        <Search className="text-blue-500" />
        <Input placeholder="ค้นหางาน..." className="border-blue-200" />
      </div>
      {[1, 2, 3].map((job) => (
        <Card key={job} className="mb-3 border-blue-100 shadow-sm">
          <CardContent className="p-3">
            <div className="flex justify-between items-center">
              <div>
                <h3 className="font-semibold">งานตัวอย่าง #{job}</h3>
                <p className="text-sm text-blue-500">รายละเอียดงานสั้น ๆ ...</p>
              </div>
              <Button className="bg-blue-600 text-white hover:bg-blue-700">ดูรายละเอียด</Button>
            </div>
          </CardContent>
        </Card>
      ))}
    </TabsContent>

    {/* Profile Tab */}
    <TabsContent value="profile" className="mt-4">
      <Card className="border-blue-100 shadow-sm">
        <CardContent className="p-4 text-center">
          <Briefcase className="mx-auto text-blue-500 mb-2" />
          <h3 className="font-semibold text-lg">เวียร์ (ผู้ใช้งาน)</h3>
          <p className="text-blue-500 mb-3">ระดับสมาชิก: มือใหม่</p>
          <div className="flex justify-center gap-1 mb-3">
            {[...Array(4)].map((_, i) => (
              <Star key={i} className="text-yellow-400" fill="gold" />
            ))}
          </div>
          <Button className="bg-blue-600 text-white hover:bg-blue-700 w-full">แก้ไขโปรไฟล์</Button>
        </CardContent>
      </Card>
    </TabsContent>
  </Tabs>

  {/* Footer */}
  <footer className="mt-6 text-xs text-blue-400">
    © 2025 WorkHub. All rights reserved.
  </footer>
</div>

); }

